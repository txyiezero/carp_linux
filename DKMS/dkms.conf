PACKAGE_NAME="carp"
PACKAGE_VERSION="1.0.0"
MAKE="make -C /lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/kmod modules"
CLEAN="make -C /lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/kmod clean"
BUILT_MODULE_NAME=carp
BUILT_MODULE_LOCATION=kmod
DEST_MODULE_LOCATION=/extra
AUTOINSTALL=yes
